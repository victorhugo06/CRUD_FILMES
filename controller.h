#ifndef CONTROLLER_H_
#define CONTROLLER_H_

static inline void cadastrar(locadora *f, int *search);

static inline void pesquisar(locadora *f, int search);

static inline void titulo(locadora *f, int search);
static inline void genero(locadora *f,int search);
static inline void avideo(locadora *f,int search);
static inline void excluir(locadora *f, int search);
static inline void alterar(locadora *f, int *search);
static inline void listar(locadora *f, int search);


#endif
